<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exoplayer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name="com.example.trailerbuzz.activities.GenrePreferenceActivity"
        android:theme="@style/Theme.Design.NoActionBar" ></activity>
        <activity
            android:name="com.example.trailerbuzz.activities.ProfileActivity"
            android:theme="@style/Theme.VideoHut" />
        <activity
            android:name="com.example.trailerbuzz.activities.HomeActivity"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.trailerbuzz.activities.RegisterActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name="com.example.trailerbuzz.activities.LoginActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name="com.example.trailerbuzz.activities.VideoPlayerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.VideoHut" />
        <activity
            android:name="com.example.trailerbuzz.activities.VideosListActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:label="@string/app_name"
            android:theme="@style/Theme.VideoHut" />
    </application>

</manifest>